// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  basePath: '/jackz-blog',

  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
}

module.exports = nextConfig
